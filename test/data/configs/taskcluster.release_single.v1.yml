version: 1
tasks:
  - $if: ' tasks_for == "github-release" '
    then:
      provisionerId: aprovisioner
      workerType: worker
      payload:
        image: "ubuntu:latest"
        command:
          - test
      metadata:
        name: "name"
        description: "description"
        owner: "test@test.com"
        source: "http://mrrrgn.com"
